# Screenshots

Add UI screenshots here for submission. Capture them by running the app locally
(`npm run dev` in both `backend/` and `frontend/`) and saving images of each screen.

Suggested set:
- `home.png` — landing page
- `services.png` — service browsing with search + category filter
- `service-detail.png` — a service with reviews
- `register.png` / `login.png` — auth
- `provider-profile.png` — provider profile editing (skills, experience, portfolio, avatar upload)
- `create-service.png` — service listing form with image upload
- `submit-request.png` — customer request form
- `project-tracking.png` — the Pending→…→Delivered workflow + updates timeline
- `customer-dashboard.png`, `provider-dashboard.png`, `admin-dashboard.png`
- `mobile.png` — a mobile/responsive view

> These cannot be auto-generated; capture them from the running app before submitting.
